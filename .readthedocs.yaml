version: 1

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.10"
    
python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
      
sphinx:
  fail_on_warning: true

formats:
  - pdf
  - epub
